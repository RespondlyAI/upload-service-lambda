/**
 * Application-wide constants and enumerations
 */

const UploadStatus = Object.freeze({
  PENDING: 'pending',
  COMPLETED: 'completed',
  FAILED: 'failed'
});

module.exports = {
  UploadStatus
};
